# RushExpress Backend

This directory will host the Django + Django REST Framework API, Channels, Celery workers, and supporting services defined in the project requirements.

## Status
Initial structure created. Application scaffolding, models, endpoints, and tests will be added next.
