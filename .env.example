# Rush Express - Environment Variables Template

# ============================================
# DATABASE CONFIGURATION (PostgreSQL)
# ============================================
POSTGRES_DB=rush_express
POSTGRES_USER=rush_express
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# ============================================
# BACKEND CONFIGURATION (Django)
# ============================================

# Django Secret Key - REQUIRED
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=CHANGE_ME_TO_RANDOM_SECRET_KEY

# Debug Mode - Set to false in production
DJANGO_DEBUG=false

# Allowed Hosts - Comma-separated list of domains
# Example: yourdomain.com,admin.yourdomain.com,rider.yourdomain.com,merchant.yourdomain.com
DJANGO_ALLOWED_HOSTS=*

# CORS Configuration - Set to false in production and configure specific origins
CORS_ALLOW_ALL_ORIGINS=false

# Database Connection (usually matches PostgreSQL config above)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis URLs for caching, Celery, and Channels
REDIS_URL=redis://redis:6379/1
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
CHANNEL_REDIS_URL=redis://redis:6379/2

# Static and Media Files
STATIC_ROOT=/static
MEDIA_ROOT=/media

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
DJANGO_LOG_LEVEL=INFO

# ============================================
# FRONTEND CONFIGURATION (Vite Build Args)
# ============================================

# Backend API URL - REQUIRED
# Example: https://yourdomain.com or https://api.yourdomain.com
VITE_API_URL=https://yourdomain.com

# WebSocket URL - REQUIRED
# Example: wss://yourdomain.com or wss://api.yourdomain.com
VITE_WS_URL=wss://yourdomain.com

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 
# 1. Replace all CHANGE_ME values with secure, random values
# 2. Update DJANGO_ALLOWED_HOSTS with your actual domain(s)
# 3. Set DJANGO_DEBUG=false in production
# 4. Set CORS_ALLOW_ALL_ORIGINS=false in production
# 5. Update VITE_API_URL and VITE_WS_URL with your actual domain
# 6. Ensure POSTGRES_PASSWORD is strong and secure
# 7. Keep DJANGO_SECRET_KEY secret and never commit it to version control
#
# For Coolify deployment:
# - Copy these variables to your Coolify project's environment variables
# - Coolify will automatically inject them into the containers
# - No need to create .env files manually
