# RushExpress Rider Web App

This directory hosts the rider-facing web app.

## UI/UX Alignment
Follow `UIReference.md` and the customer app (`web-customer`) design tokens to keep colors, typography, spacing, and components consistent.

## Status
Initial structure created. Implementation will follow the shared RushExpress UI/UX reference.
